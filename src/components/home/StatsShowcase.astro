---
import { Image } from 'astro:assets';
import AnimatedStats from '../react/AnimatedStats';
import logisticsNetworkImg from '@assets/photo-1578575437130-527eed3abbec.jpg';
---

<section class="relative py-24 overflow-hidden">
  <!-- Background -->
  <div class="absolute inset-0">
    <Image 
      src={logisticsNetworkImg}
      alt="Global logistics network"
      class="w-full h-full object-cover"
      width={2070}
      height={1380}
    />
    <div class="absolute inset-0 bg-slate-900/90"></div>
  </div>

  <!-- Content -->
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center mb-16">
      <div class="inline-block px-4 py-2 mb-4 bg-white/10 backdrop-blur-sm text-white text-sm font-semibold rounded-full">
        Respaldo Operativo Certificado
      </div>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        Potencia Estructural para <span class="text-transparent bg-clip-text bg-linear-to-r from-red-400 to-red-600">Líderes Industriales</span>
      </h2>
      <p class="text-xl text-slate-300 max-w-3xl mx-auto leading-relaxed">
        Garantizamos la continuidad y escalabilidad de su operación mediante infraestructuras diseñadas bajo los más altos estándares de precisión e ingeniería.
      </p>
    </div>

    <AnimatedStats client:load />

    <!-- Trusted By Section -->
    <div class="mt-20">
      <p class="text-center text-slate-400 text-sm font-semibold uppercase tracking-wider mb-8">
        Confiado por Empresas de Prestigio
      </p>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        {/* Client logos placeholder */}
        <div class="flex items-center justify-center group">
          <div class="w-36 h-20 bg-white/5 hover:bg-white/10 backdrop-blur-sm rounded-lg flex items-center justify-center text-white/40 group-hover:text-white/60 font-semibold transition-all border border-white/10 hover:border-white/20">
            BACKUS
          </div>
        </div>
        <div class="flex items-center justify-center group">
          <div class="w-36 h-20 bg-white/5 hover:bg-white/10 backdrop-blur-sm rounded-lg flex items-center justify-center text-white/40 group-hover:text-white/60 font-semibold transition-all border border-white/10 hover:border-white/20">
            APEU
          </div>
        </div>
        <div class="flex items-center justify-center group">
          <div class="w-36 h-20 bg-white/5 hover:bg-white/10 backdrop-blur-sm rounded-lg flex items-center justify-center text-white/40 group-hover:text-white/60 font-semibold transition-all border border-white/10 hover:border-white/20">
            MOLICENTRO
          </div>
        </div>
        <div class="flex items-center justify-center group">
          <div class="w-36 h-20 bg-white/5 hover:bg-white/10 backdrop-blur-sm rounded-lg flex items-center justify-center text-white/40 group-hover:text-white/60 font-semibold transition-all border border-white/10 hover:border-white/20">
            UNICON
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


