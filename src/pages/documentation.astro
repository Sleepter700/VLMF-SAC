---
import Layout from '../layouts/BaseLayout.astro';
import { Hammer, HardHat, Construction, ArrowLeft } from 'lucide-react';
---

<Layout title="Sitio en Construcción | VLM&F Asociados">
  <main class="min-h-screen bg-slate-50 flex items-center justify-center p-4 overflow-hidden relative">
    <div class="absolute -bottom-24 -right-24 w-96 h-96 bg-red-600/5 rounded-full blur-3xl"></div>
    
    <div class="max-w-2xl w-full text-center relative z-10">
      
      <div class="flex justify-center mb-8">
        <div class="relative">
          <div class="absolute inset-0 animate-ping bg-red-100 rounded-full opacity-75"></div>
          <div class="relative bg-white p-6 rounded-3xl shadow-2xl border border-slate-100">
            <Construction className="w-16 h-16 text-red-600" strokeWidth={1.5} />
          </div>
        </div>
      </div>

      <h1 class="text-5xl md:text-6xl font-black text-slate-900 mb-4 tracking-tighter">
        BAJO <span class="text-transparent bg-clip-text bg-linear-to-r from-red-600 to-red-800">MONTAJE</span>
      </h1>
      
      <p class="text-xl text-slate-600 mb-10 leading-relaxed max-w-lg mx-auto">
        Estamos optimizando nuestra plataforma digital para ofrecerle una experiencia a la altura de nuestra <strong>ingeniería estructural</strong>.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-12">
        <div class="bg-white p-4 rounded-xl shadow-sm border border-slate-100">
          <HardHat className="w-5 h-5 text-red-600 mx-auto mb-2" />
          <span class="text-xs font-bold uppercase tracking-widest text-slate-400">Estatus</span>
          <p class="font-semibold text-slate-900">Obra Civil</p>
        </div>
        <div class="bg-white p-4 rounded-xl shadow-sm border border-slate-100">
          <Hammer className="w-5 h-5 text-red-600 mx-auto mb-2" />
          <span class="text-xs font-bold uppercase tracking-widest text-slate-400">Avance</span>
          <p class="font-semibold text-slate-900">85%</p>
        </div>
        <div class="bg-white p-4 rounded-xl shadow-sm border border-slate-100">
          <div class="w-2 h-2 bg-green-500 rounded-full mx-auto mb-3 animate-pulse"></div>
          <span class="text-xs font-bold uppercase tracking-widest text-slate-400">Servidor</span>
          <p class="font-semibold text-slate-900">Operativo</p>
        </div>
      </div>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a 
          href="javascript:history.back()" 
          class="flex items-center px-6 py-3 text-slate-600 font-semibold hover:text-red-600 transition-colors group"
        >
          <ArrowLeft className="w-4 h-4 mr-2 group-hover:-translate-x-1 transition-transform" />
          Volver atrás
        </a>
        <a 
          href="https://wa.me/51920872504" 
          class="px-8 py-3 bg-slate-900 text-white font-bold rounded-lg shadow-lg hover:bg-red-600 transition-all hover:-translate-y-1"
        >
          CONTACTO DIRECTO
        </a>
      </div>

      <div class="mt-16 pt-8 border-t border-slate-200">
        <p class="text-xs text-slate-400 font-medium uppercase tracking-[0.2em]">
          VLM&F Asociados • Ingeniería y Construcción
        </p>
      </div>
    </div>
  </main>
</Layout>

<style>
  /* Efecto de líneas de construcción en el fondo */
  main {
    background-image: radial-gradient(circle at 2px 2px, #e2e8f0 1px, transparent 0);
    background-size: 40px 40px;
  }
</style>